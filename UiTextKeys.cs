namespace Naufal_Windows_Tech_s_Powertoys;

// Canonical keys shared by generated translations and UI callers.
internal static class UiTextKeys
{
    internal const string RepairSelected = "Repair selected";
    internal const string EnableSelected = "Enable selected";
    internal const string DownloadInstall = "Download & install";
    internal const string AnalyzeReload = "Analyze / reload";
    internal const string SelectSafe = "Select safe";
    internal const string ApplySelected = "Apply selected";
    internal const string RuntimeInstallConfirmation = "Download, verify, and silently install the selected official gaming runtime?";
    internal const string RuntimeRepairConfirmation = "Download the official package and repair/re-install the selected runtime? Existing components are refreshed without an uninstall.";
    internal const string RuntimeEnableConfirmation = "Enable or restore the selected Windows prerequisite to its compatible baseline?";
}
